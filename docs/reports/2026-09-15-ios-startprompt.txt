Lees docs/reports/2026-09-15-ios-app.html, apps/ios/README.md, CLAUDE.md en de iOS-besluiten in docs/DECISIONS.md. Controleer eerst git status en de huidige main: de verbindingstest en de app zijn al gebouwd. Bouw ze niet opnieuw vanaf nul.

Bas heeft opdracht gegeven de app af te maken. De eerdere pauze na iedere fase geldt niet meer. Werk autonoom door aan implementatie, integratie en noodzakelijke tests. Gebruik uitsluitend Codex-sub-agents, geen Claude. Geef regelmatig een korte voortgangsupdate. Respecteer het werk van andere agents en laat één integrator commits maken.

Doel: native iPhone- en iPad-app in apps/ios, minimum iOS/iPadOS 26, bundle ID app.ruimte.mobile. SwiftUI, UIKit voor canvas/timeline en SwiftTerm voor terminal. Geen lokale daemon. Eén ed25519-sleutel en SessionVault voor sessies en machines, callback ruimte://pulsar/callback, koppellinks uitsluitend HTTPS. Bestaande canvasnodes mogen niet worden verplaatst of geresized. Browserpagina's hebben geen privileged bridge.

Verbind met normale ICE-selectie, zoals de andere clients: direct wanneer mogelijk, TURN als terugval. Forceer relay alleen met de expliciete diagnostische schakelaar. Verifieer altijd de pinned machine key, gesigneerde signaling, channelBinding en protocolversie. De bestaande Apple Worker-redirectflow blijft de loginroute; native Apple-tokenuitwisseling is afzonderlijk vervolgwerk.

Controleer de volledige gegenereerde daemon-API en gedeelde MachineClient, projectnavigatie en merges, chat inclusief streaming/composer/approvals, terminal follow zonder resize, canvas, bestanden/Git/processen/usage, encrypted notifications en ActivityKit. Bewaar onbekende node- en viewsoorten. Controleer dat meerdere iPad-vensters dezelfde machineverbinding en attachments delen en elkaar niet afmelden. Het sluiten van een pagina mag geen sessie op de machine stoppen.

Push gebruikt X25519 + HKDF-SHA256 + AES-256-GCM, canonieke routing-AAD en een ed25519-machinehandtekening. Controleer de gedeelde Bun/CryptoKit-fixture, account/session-binding, intrekking, replay, expiry en limieten. Een achtergrondantwoord krijgt een begrensde poging en een route naar de sessie als die niet lukt. Alleen de toegestane Live Activity-titel en fase reizen onversleuteld.

Bouw met XcodeGen en test met swift test en xcodebuild test op een geïnstalleerde simulator. Xcode/SwiftTerm vraagt de Metal Toolchain. Gebruik ad-hoc signing voor echte simulator-Keychain-tests. project.yml blijft de bron; het gegenereerde Xcode-project en Package.resolved staan in git voor Xcode Cloud. Geen package.json in apps/ios, geen buildoutput of xcuserdata in git. Bij TypeScript-wijzigingen: bun run format, bun run check, bun test en bun install --frozen-lockfile. Gebruik deterministische fakes voor timingtests.

Werk het HTML-rapport, README en besluiten bij met werkelijke testresultaten. Bekijk de native schermen en het rapport visueel. Benoem afzonderlijk wat nog alleen op hardware of met externe Apple-configuratie te verifiëren is: Wi-Fi/5G/TURN, achtergrondacties, iPad-multitasking, performance, APNs-bezorging en distributiesigning/TestFlight. Geen ongeteste acceptatieclaim en geen productie-deployment of upload. Commit afgerond werk op main met Engelse conventional commits, zonder attributie; niet pushen.
