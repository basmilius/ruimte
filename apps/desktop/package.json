{
    "name": "@ruimte/desktop",
    "productName": "Ruimte",
    "private": true,
    "version": "0.0.0",
    "description": "Terminals, agents and browsers on one infinite canvas",
    "author": "Bas Milius",
    "homepage": "https://ruimte.app",
    "type": "module",
    "main": "dist/main.cjs",
    "scripts": {
        "build:main": "bun build src/main.ts --target=node --format=cjs --external electron --outfile dist/main.cjs && bun build src/preload.ts --target=node --format=cjs --external electron --outfile dist/preload.cjs",
        "dev": "bun run build:main && RUIMTE_DEV_URL=http://localhost:5173 bun scripts/launch.ts",
        "smoke": "bun run build:main && RUIMTE_DEV_URL=http://localhost:5173 RUIMTE_SMOKE=1 bun scripts/launch.ts",
        "build": "bun run build:main",
        "icon": "bun scripts/icon.ts",
        "typecheck": "tsc --noEmit",
        "start": "bun run build:main && bun scripts/launch.ts",
        "pack": "bun run build:main && electron-builder --publish never",
        "pack:dir": "bun run build:main && electron-builder --dir --publish never"
    },
    "devDependencies": {
        "@types/node": "^24.13.3",
        "electron": "^44.3.0",
        "electron-builder": "^26.15.3",
        "electron-updater": "^6.6.2",
        "typescript": "~6.0.2"
    }
}
