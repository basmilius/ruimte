import { createContext } from 'react';

/* Whether the code blocks below are still being written, which a reply says for the fence that has not closed. */
export const CodeStreamingContext = createContext(false);
