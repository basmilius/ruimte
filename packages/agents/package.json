{
  "name": "@ruimte/agents",
  "version": "0.0.0",
  "description": "Ruimte's AI chat host for Node: Claude Code and Codex behind one chat thread, provider accounts and usage, served over a port.",
  "type": "module",
  "exports": {
    "./*.json": "./src/*.json",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ruimte/agent-contracts": "workspace:*",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
