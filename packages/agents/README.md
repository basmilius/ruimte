# @ruimte/agents

The server side of Ruimte's AI chat that knows nothing of Ruimte. It runs in Bun and in Node, Electron's utility process included, and never uses a Bun API outside its tests.

Import per file: `@ruimte/agents/<path under src>`, without the extension.
